# GraphQL Introduction Project

This project lists a list of gifs when searching for "fun" on giphy.

## How to install

* Install [meteorjs](https://www.meteor.com/)
* Clone / download this folder
* Go into project folder and run

```bash
meteor npm install
meteor # this will execute the meteor server
```

